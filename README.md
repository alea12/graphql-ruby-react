# GraphQL Ruby (Backend) + React (Frontend) Test Application

## Backend

```sh
$ cd backend
$ bundle install && rails db:setup && rails s -p 4000
```

## Frontend

```sh
$ cd frontend
$ yarn install
$ yarn start
```
